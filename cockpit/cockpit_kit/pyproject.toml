[project]
name = "cockpit-kit"
version = "0.1.0"
description = "Shared Streamlit cockpit components for x402 agents"
requires-python = ">=3.11"
dependencies = [
    # The governance layer, from PyPI. An example that only works from a
    # particular checkout layout is not an example.
    "aegoll>=0.1.0","streamlit>=1.40", "httpx>=0.27", "x402-core"]

[tool.setuptools.packages.find]
include = ["cockpit_kit*"]
